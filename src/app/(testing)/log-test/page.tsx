/*
URL = "/log-test"

Notes:
- (testing) is a private folder, not part of the route path

*/

type ResponseData = {
  code: number,
  name: string
}

export default function LogTest (
  //req: NextApiRequest,
  //res: NextApiResponse<ResponseData>
  ) {
      //res.status(200).json({ code: 111, name: 'test' })
      return <h1>FIX THIS TEST RESPONSE!!</h1>
  }  